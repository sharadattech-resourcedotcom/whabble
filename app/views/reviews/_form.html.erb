<%= form_with(model: review, local: true) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reviewtitle %>
    <%= form.text_field :reviewtitle, id: :review_reviewtitle %>
  </div>

  <div class="field">
    <%= form.label :reviewscore %>
    <%= form.number_field :reviewscore, id: :review_reviewscore %>
  </div>

  <div class="field">
    <%= form.label :reviewtext %>
    <%= form.text_area :reviewtext, id: :review_reviewtext %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
