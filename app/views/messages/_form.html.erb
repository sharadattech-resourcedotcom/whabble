<%= form_with(model: message, local: true) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :messagesubj %>
    <%= form.text_field :messagesubj, id: :message_messagesubj %>
  </div>

  <div class="field">
    <%= form.label :messagecontent %>
    <%= form.text_field :messagecontent, id: :message_messagecontent %>
  </div>

  <div class="field">
    <%= form.label :messagerecipients %>
    <%= form.text_field :messagerecipients, id: :message_messagerecipients %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
